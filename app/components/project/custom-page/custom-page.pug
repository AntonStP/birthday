extends ../../framework/page/page

append constants
  if php
    include ../utils/php-utils/php-utils.php

append mixins
  include ../../framework/framework
  include ../project

append body_before
  //-script.
      if (window !== top) { document.body.className += ' _iframe'; }
  //-include _svg-symbols
  //-+bitrix-panel()


append modal


block page
  .page.custom-page.parallax-container(data-parallax-settings="20")
    block content
    .section.section_fixed
      +pagination(copyright.pagination)
